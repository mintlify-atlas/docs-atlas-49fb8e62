---
title: 'Product Model'
description: 'Complete reference for the Product domain model including price validation and value object patterns'
---

# Product Model

The `ProductoPOJO` represents a product entity in the e-commerce system. It encapsulates product information including identification, name, and price with built-in validation through value objects.

## Model Structure

<CodeGroup>
```json Example Response
{
  "id": 67890,
  "nombre": "Laptop Dell XPS 15",
  "precio": {
    "value": 1299.99
  }
}
```

```java Java Model
public class ProductoPOJO {
    private Long id;
    private String nombre;
    private PrecioVO precio;
}
```
</CodeGroup>

## Fields

<ResponseField name="id" type="Long" required>
  Unique identifier for the product.
  
  <Expandable title="Details">
    - Auto-generated by the database
    - Primary key for product records
    - Used for product lookups and inventory management
    - Referenced in orders and cart items
  </Expandable>
</ResponseField>

<ResponseField name="nombre" type="String" required>
  Product name or title.
  
  <Expandable title="Details">
    - Human-readable product identifier
    - Displayed in product listings and search results
    - Should be descriptive and unique
    - Recommended to include brand and key features
  </Expandable>
</ResponseField>

<ResponseField name="precio" type="PrecioVO" required>
  Price value object with built-in validation to ensure price integrity.
  
  <Expandable title="PrecioVO Structure">
    <ResponseField name="value" type="Float" required>
      The actual price amount with strict validation rules.
      
      **Validation Rules:**
      - **Not null:** Price value cannot be null
      - **Non-negative:** Price must be greater than or equal to 0
      - **Float precision:** Supports decimal values for cents/fractional currency
      
      **Business Logic:**
      - Value object pattern ensures immutability
      - Price updates create new `PrecioVO` instances
      - Prevents invalid price states throughout the application
    </ResponseField>
  </Expandable>
</ResponseField>

## Price Validation

<Note>
  The `PrecioVO` value object enforces price integrity through constructor validation. Any attempt to create an invalid price will throw an `IllegalArgumentException`.
</Note>

### Validation Rules

The price must satisfy ALL of the following conditions:

| Rule | Requirement | Exception Message |
|------|-------------|------------------|
| **Not Null** | Price value cannot be null | "El precio no puede ser nulo" |
| **Non-Negative** | Price must be >= 0 | "El precio no puede ser negativo" |

### Valid Price Examples

```json
// Valid prices
{
  "precio": { "value": 0.0 }      // Free item
}
{
  "precio": { "value": 9.99 }     // Standard pricing
}
{
  "precio": { "value": 1299.99 }  // High-value item
}
{
  "precio": { "value": 0.01 }     // Minimum non-zero price
}
{
  "precio": { "value": 999999.99 } // Large price
}
```

### Invalid Price Examples

```json
// Null value
{
  "precio": { "value": null }
}  // IllegalArgumentException: "El precio no puede ser nulo"

// Negative price
{
  "precio": { "value": -10.50 }
}  // IllegalArgumentException: "El precio no puede ser negativo"

// Missing precio object
{
  "nombre": "Product Name"
}  // Validation error
```

## Creating a Product

<CodeGroup>
```json Request Body
{
  "nombre": "Wireless Mouse",
  "precio": {
    "value": 29.99
  }
}
```

```java Java Code
ProductoPOJO producto = new ProductoPOJO();
producto.setNombre("Wireless Mouse");
producto.setPrecio(new PrecioVO(29.99f));
```
</CodeGroup>

## Updating a Price

The `PrecioVO` provides an `actualizarPrecio()` method that creates a new price value object:

```java
PrecioVO currentPrice = producto.getPrecio();
PrecioVO newPrice = currentPrice.actualizarPrecio(24.99f);
producto.setPrecio(newPrice);
```

<Note>
  Price value objects are immutable. The `actualizarPrecio()` method returns a new `PrecioVO` instance rather than modifying the existing one. This ensures price history and audit trails remain consistent.
</Note>

## Complete Product Example

<CodeGroup>
```json Full Product Object
{
  "id": 12345,
  "nombre": "Samsung Galaxy S24 Ultra",
  "precio": {
    "value": 1199.99
  }
}
```

```json Product List Response
[
  {
    "id": 1,
    "nombre": "iPhone 15 Pro",
    "precio": { "value": 999.00 }
  },
  {
    "id": 2,
    "nombre": "MacBook Pro 16\"",
    "precio": { "value": 2499.00 }
  },
  {
    "id": 3,
    "nombre": "AirPods Pro",
    "precio": { "value": 249.00 }
  }
]
```

```java Creating Multiple Products
List<ProductoPOJO> productos = Arrays.asList(
    new ProductoPOJO(1L, "iPhone 15 Pro", new PrecioVO(999.00f)),
    new ProductoPOJO(2L, "MacBook Pro 16\"", new PrecioVO(2499.00f)),
    new ProductoPOJO(3L, "AirPods Pro", new PrecioVO(249.00f))
);
```
</CodeGroup>

## Price Display and Formatting

The `PrecioVO` class overrides `toString()` to return the string representation of the price value:

```java
PrecioVO precio = new PrecioVO(99.99f);
System.out.println(precio.toString());  // Output: "99.99"
```

<Note>
  For currency formatting with symbols ($, €, etc.) and locale-specific formatting, implement formatting logic in your presentation layer or API response serializer.
</Note>

## Business Rules

### Free Products

Products with a price of `0.0` are considered free:

```json
{
  "id": 999,
  "nombre": "Free Sample Pack",
  "precio": { "value": 0.0 }
}
```

### Price Updates

<Warning>
  When updating product prices:
  - Always create a new `PrecioVO` instance
  - Log price changes for audit purposes
  - Consider implementing price history tracking
  - Validate business rules (e.g., maximum discount percentages)
</Warning>

### Equality and Comparison

Two `PrecioVO` objects are equal if their values are equal:

```java
PrecioVO precio1 = new PrecioVO(99.99f);
PrecioVO precio2 = new PrecioVO(99.99f);
PrecioVO precio3 = new PrecioVO(89.99f);

precio1.equals(precio2);  // true
precio1.equals(precio3);  // false
```

## API Integration Examples

### GET /productos/{id}

```json Response
{
  "id": 42,
  "nombre": "Mechanical Keyboard RGB",
  "precio": {
    "value": 149.99
  }
}
```

### POST /productos

```json Request
{
  "nombre": "USB-C Hub 7-in-1",
  "precio": {
    "value": 49.99
  }
}
```

```json Response (201 Created)
{
  "id": 543,
  "nombre": "USB-C Hub 7-in-1",
  "precio": {
    "value": 49.99
  }
}
```

### PUT /productos/{id}

```json Request
{
  "nombre": "USB-C Hub 7-in-1 (Updated)",
  "precio": {
    "value": 39.99
  }
}
```

## Error Handling

### Invalid Price Errors

```json 400 Bad Request - Null Price
{
  "error": "IllegalArgumentException",
  "message": "El precio no puede ser nulo",
  "field": "precio.value"
}
```

```json 400 Bad Request - Negative Price
{
  "error": "IllegalArgumentException",
  "message": "El precio no puede ser negativo",
  "field": "precio.value",
  "provided": -25.00
}
```

## Related Models

- [User Model](/api/models/user-model) - User entity with password validation

## Source Code Reference

- `ProductoPOJO`: src/main/java/com/example/demo/producto/domain/models/ProductoPOJO.java:11
- `PrecioVO`: src/main/java/com/example/demo/producto/domain/vo/PrecioVO.java:6
